export async function GET() {
  return Response.json([
    "Increase tummy time to build core strength.",
    "Try adding a soft lullaby before bedtime.",
    "Maintain consistent feeding schedule.",
  ]);
}
